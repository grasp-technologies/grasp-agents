"""
E2B cloud-sandbox backend: file + exec surfaces, persistent shell, and
Jupyter kernel (code-interpreter), all over one live sandbox handle.

Implementations behind the shared seams in the parent package
(:mod:`..exec_backend`, :mod:`..kernel`, :mod:`..environment`).
"""

from __future__ import annotations
